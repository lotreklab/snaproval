# Minimal environment file for local development
# SNAPROVAL_HOSTNAME is used by the Caddyfile. Default is localhost.
SNAPROVAL_HOSTNAME=localhost
# PORT for the backend (server/standalone.js falls back to 3000 if unset)
PORT=3000
# Prisma expects sqlite URLs to start with the file: protocol
DATABASE_URL=file:./dev.db